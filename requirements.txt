# =============================================================================
# Epithelial Robustness Collapse — Python Dependencies
# Install: pip install -r requirements.txt
#
# For an exact, previously-verified-working set of pinned versions (incl.
# transitive dependencies), see requirements.lock.txt instead — these are
# floors, so what actually resolves here can drift over time and
# scanpy/UMAP/Leiden/Harmony are all version-sensitive even with a fixed
# random seed (config.yaml: random_seed: 42).
# =============================================================================

# --- Core scientific stack ---
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# --- Single-cell genomics ---
scanpy>=1.9.6
anndata>=0.10
leidenalg>=0.10
python-igraph>=0.11
scikit-misc>=0.2      # required by scanpy's highly_variable_genes(flavor="seurat_v3"),
                       # which feature_selection.method in config.yaml uses; not
                       # previously listed even though the pipeline depends on it

# --- Visualization ---
matplotlib>=3.7
seaborn>=0.13

# --- Statistics ---
statsmodels>=0.14

# --- Batch correction ---
harmonypy>=0.0.9
# scvi-tools>=1.0        # optional; install separately if GPU available

# --- Trajectory inference ---
# palantir>=1.3          # optional; install if using Palantir trajectory

# --- Gene set utilities ---
gseapy>=1.1

# --- Configuration ---
pyyaml>=6.0

# --- Notebooks ---
jupyter>=1.0
jupyterlab>=4.0
nbformat>=5.9

# --- Utilities ---
tqdm>=4.65

# --- Testing ---
pytest>=7.4
